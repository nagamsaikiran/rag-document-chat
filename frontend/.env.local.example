# Copy to .env.local. Where the FastAPI backend is reachable.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: Google Analytics 4 Measurement ID (e.g. G-XXXXXXXX).
# Leave blank for local dev; set it for your deployed site to enable analytics.
NEXT_PUBLIC_GA_ID=
